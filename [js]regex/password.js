// 密码规则匹配
// 第一个括号，断言后面有任意个任意字符，但至少出现一个小写字母。后面2个括号相似
// 第4个括号，断言只能出现指定组合的字符
// .{8,16}$ 匹配任意8到16个字符
const reg = /(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[~!@#$%^&*])(?=^[a-zA-Z0-9~!@#$%^&*]+$).{8,16}$/
reg.test('ASFDdge35is^$') // true
